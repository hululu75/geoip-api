# GeoIP API Configuration

# Port to expose the service (default: 8080)
HOST_PORT=8080

# Directory containing GeoIP database file on the host machine
# Download from: https://dev.maxmind.com/geoip/geolite2-free-geolocation-data
# Default: ./data (project root/data directory)
GEOIP_DB_DIR=/path/to/geoip-data

# GeoIP database filename (default: GeoLite2-Country.mmdb)
# Other options: GeoLite2-City.mmdb, GeoLite2-ASN.mmdb
GEOIP_DB_FILENAME=GeoLite2-Country.mmdb

# Container internal port (default: 8080)
CONTAINER_PORT=8080

# Cache configuration
# Enable or disable cache (default: false)
#
# When to enable cache:
#   - High traffic (>1000 req/s) with repeated IPs
#   - Expected cache hit rate >70%
#   - Remote or slow storage
#
# When to disable cache:
#   - Low traffic (<100 req/s)
#   - Mostly unique IPs (cache hit rate <30%)
#   - Local SSD with OS caching
#
# Benchmark your use case: ./test_cache.sh
CACHE_ENABLED=false

# Maximum number of cached IP lookups (default: 10000)
# Memory usage: ~1MB per 10000 items
CACHE_MAX_SIZE=10000
